<section id="join">
  <div class="container">
    <div class="mb-3">
      <h2><%- __(`post.join.${page.join.type}.title`) %></h2>
      <span><%- __(`post.join.${page.join.type}.content`) %></span>
    </div>
    <img src="<%- url_for(page.join.image) %>" class="w-100" />
  </div>
</section>