<section id="cover">
  <div class="container">
    <h1><%- page.title %></h1>
    <p><%- page.description %></p>
    <% if (page.cover) { %>
    <div class="embed-iframe">
      <iframe src="<%- embed_link(page.cover) %>"></iframe>
    </div>
    <% } else { %>
      <div class="image-16-9" style="background-image: url('<%- url_for(page.thumbnail) %>')"></div>
    <% } %>
  </div>
  <div class="bg bg-<%- (page.color || '').toLowerCase() %>"></div>
</section>